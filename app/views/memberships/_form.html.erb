<%= form_with(url: organization_membership_path(@organization, membership), model: membership) do |form| %>
  <% if membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
        <% membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <label class="label cursor-pointer flex items-center justify-start gap-4">
      <span class="label-text">Admin</span>
      <%= form.radio_button :role, 'admin', class: "radio radio-sm" %>
    </label>
    <label class="label cursor-pointer flex items-center justify-start gap-4">
      <span class="label-text">Member</span>
      <%= form.radio_button :role, 'member', class: "radio radio-sm" %>
    </label>

  <div class="flex items-center justify-end">
    <%= form.submit class: "btn btn-accent btn-xs" %>
  </div>
<% end %>
